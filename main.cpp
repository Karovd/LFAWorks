#include "Graph.h"
using namespace std;

int main(int argc, char** argv){
	Grafo *G;
	fstream file;
	int auto_iterator=0;
	string word;

	if(!file.open(argv[1], ios::in | ios::out)){
		cout<<"Erro ao abrir o arquivo!"<<endl;
	} else {
		auto_iterator=0;
		while(!file.eof()){
					
		
	return 0;
}
